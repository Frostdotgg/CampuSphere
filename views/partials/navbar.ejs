<nav class="navbar" id="navbar">
    <div class="container navbar__inner">
        <!-- Brand -->
        <a href="/" class="navbar__brand">
            <img src="/img/cspc-logo.png" alt="CSPC Logo" class="navbar__logo">
            <span class="navbar__title">CampuSphere</span>
        </a>

        <!-- Navigation Links -->
        <div class="navbar__links" id="navLinks">
            <a href="/" class="navbar__link">Home</a>
            <a href="/#features" class="navbar__link">Features</a>
            <div class="navbar__actions">
                <% if (typeof user !== 'undefined' && user) { %>
                    <a href="/dashboard" class="navbar__cta">Dashboard</a>
                <% } else { %>
                    <a href="/auth" class="navbar__cta">Login</a>
                <% } %>
            </div>
        </div>

        <!-- Mobile Toggle -->
        <button class="navbar__hamburger" id="hamburger" type="button"
            aria-controls="navLinks" aria-expanded="false"
            aria-label="Open navigation menu">
            <span></span>
            <span></span>
            <span></span>
        </button>
    </div>
</nav>
<%# SINGLE owner of the #hamburger/#navLinks state for every anonymous page that
    includes this partial (landing and /privacy). Page-local hamburger handlers
    must NOT be reintroduced — see public/js/public-nav.js. %>
<script src="/js/public-nav.js" defer></script>
