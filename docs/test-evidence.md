# CampuSphere Test Evidence Checklist

Milestone 8, Section 8.10. Use this checklist to record defense and deployment
evidence without committing private data. Store actual screenshots or recordings
outside the repo unless they have been reviewed and explicitly approved.

## 2026-08-13 Current Evidence Classification

| Evidence class | Current disposition |
| --- | --- |
| Git/deployment | Final R8 authority synchronization `fea3b2e11c6331eddc1ee091b165427d8e0218d7` remains the accepted technical Production baseline. The later documentation/static-assertion-only synchronization `db05b549807535840968bf28cdefac4154a6d59d` is committed and pushed on `main`; live Git confirmed local HEAD equal to origin/main with a clean index/worktree. Vercel built `db05b54` as `Ready` / `Production` / `Staged`, custom-domain assignment was `Skipped`, and it was not promoted or made `Current`; `https://campusphere-cspc.vercel.app` remained on `fea3b2e` |
| Post-deployment verification | PASS within bounded anonymous read-only GET-only scope: owner-observed Vercel `Ready` / `Production` / `Current`, branch `main`, source `fea3b2e`, 17-second completed build; expected public responses/assets and sampled source-byte identity; protected HTML `302 -> /auth`; protected JSON `401`; zero checked `Set-Cookie`; `/auth`, authentication, and schedule auditing deliberately excluded |
| Deployment promotion control | Owner-observed Vercel Production Branch Tracking shows `Auto-assign Custom Production Domains` **Disabled**. Future `main` pushes may create staged Production deployments but require explicit manual promotion before replacing the live alias. Saved-state evidence only; not tested by a dummy push |
| Historical SEC-51/R8 baseline | Commit `bbb25d0dee5917e4704da35784421c840f825afb` is accepted history; historical/superseded: before `fea3b2e`, Production served `0627bf78228148e3f989275810c333c16a1f3356`, with five-file verification `75/75`, `3777/3777`, five-stage QA, `24/24 -> 18/18 -> 46/46`, and anonymous smoke `31/31`; same-author/self-review caveat retained |
| Automated frozen-data rehearsal | PASS with three isolated contexts; one temporary CSPC student and one temporary Gmail guest created and removed; seven-user baseline and session/freeze postconditions restored; automated evidence only, not human pilot |
| Rehearsal deviations | Retained: pilot PII printed to executor transcript by an over-broad read; three temporary repo files created then removed; one corrected read-only misclick; human sign-in sequencing interruptions |
| Human pilot | **OWNER-ACCEPTED — 2026-08-05; zero reported findings.** Participant/Form evidence remains external and no participant PII is recorded in Git. The tested build's full source-commit identity was not independently verified, so this is owner-attested pilot acceptance rather than independent current-build verification. Pilot review is complete for sequencing purposes |
| Expanded Guided-VR candidate | MySQL: 34 buildings, 44 route nodes, 100 directed edges, 50 reverse pairs, 100 valid geometries, 671 scenes, 1,396 hotspots. Supabase: 25, 26, 50, 25, 50, 664, 1,372. Shared catalog: 25 active destinations, 472 configured steps, 99 unique scene keys; BE.6 `46/46` |
| Migrations | Exactly `0001-0019`; no `0020` |
| Backup/restore | External backup manifest verifies 109/109 files with zero mismatches; provider-supported Supabase and MySQL final-delta dumps restored in isolated disposable targets; 86 referenced Cloudinary delivery assets exported and hashed, explicitly not a management/original-account export. The post-duplicate-correction MySQL backup separately passes 6/6 checksum/isolated canonical restore-redump checks |
| Session postcondition | The earlier supported Supabase student-session restoration remains historical. Current bounded restoration: a fail-closed preflight reverified exactly two unexpired sessions for the one intended-role canonical MySQL student, zero for the canonical MySQL administrator and all four canonical Supabase identities, and the explicitly selected MySQL session store. Exactly one supported `revokeUserSessions()` call removed both student sessions; no direct session-row deletion, account/application-data change, or broad cleanup occurred. Current ordered postconditions are **`24/24 -> 18/18 -> 46/46`** |
| Independent candidate review | Prior candidate manifest SHA-256 `b4c2c3c2a5766399b843c6e43f2f8cf347bcc04473e5ba6a0a808397c77a3d56` received commit-readiness NO-GO for an incomplete ordered CAS sequence guard, contradictory SEC-37 package evidence, obsolete OFF.3 catalog scope, and premature pilot sequencing. Exact 33-file manifest SHA-256 `2f78d9754094572ac2b6a2bec02786d66b35a651141cd8c0f5705ac85d1282a8` then received NO-GO because the exact package pin was not enforced live, obsolete handoff policy was not isolated from current authority, and current dates were stale. Exact 34-file manifest SHA-256 `ebf1142c11e3c027c0b3339a6888bc19196936ae3323644d907c68def224c4b4` then received NO-GO because current authority gave the same rejected 4,628-PASS retry both its transcript-faithful nine wording failures plus residue and a duplicate incorrect lower count. Exact 11-file manifest SHA-256 `4d37507071089be4f6ce92404465a28334f9a03dbad82d02dfde2b013c3183ad` later received R8 NO-GO solely for self-expiring lifecycle authority in the current reusable Claude prompt and incomplete lifecycle-matcher coverage. These dispositions are historical; live Git and the latest external review report control the corrected bytes' disposition |
| Current package boundary | **M12.P1-D6 review-correction candidate: 168 files, 7,042,705 bytes, aggregate SHA-256 `fe08232edf026edcbd33371df7d484bfaf39e3de0dafe22f5144e18e08efbf2b`; focused package verification `74/74`. The file count is unchanged from the preceding D6 candidate; only the four corrected runtime/view files changed size. Historical/rejected after the independent review, never accepted: the first D6 candidate at 168 files, 7,022,574 bytes, aggregate SHA-256 `779d331824026ce0c1c9510e6393790d0a8da508498a395c1e97d9a04c19e7fd`, whose 15-file manifest was `a6202b0f2106f244d58a41fbc1d646f360356df299790d5f88d44fe2729a2bc2`. Historical/superseded, never accepted: the OFF.3-OFF.5 2D offline-navigation candidate at 165 files, 6,971,229 bytes, aggregate SHA-256 `e383f2fe708c5233192ec3602727ed2029dbc906df1ad53a75a70f6fa583334b`. Accepted technical Production predecessor remains 158 files, 6,245,074 bytes, aggregate SHA-256 `b3113c05daaa5d2e870f204083923434456580fa6499190421de062ce9cabbd4` on `fea3b2e11c6331eddc1ee091b165427d8e0218d7`, and Production stays on that pre-offline baseline. No deployment/promotion is authorized.** |
| Offline candidate full verification | **Historical/rejected: `npm test` exited 1 after `4635/4641` with exactly six static documentation/authority failures and no `QUALITY-GATES OK`.** Every executed runtime, database, catalog, BE.6, and final embedded `18/18` residue check was green. Fail-closed sequencing stopped before `npm run qa` and before the standalone `24/24 -> 18/18 -> 46/46` postconditions. The bounded correction has focused static evidence only, no Codex GO, and still requires independent review plus separately authorized replacement full verification; no session or data correction was required |
| Historical pre-production verification boundary | **Accepted predecessor evidence.** The first integrated read-only M12.P1-R8 review of clean commit `43627cf` reverified `npm test` `4641/4641` with `QUALITY-GATES OK`, full five-stage `npm run qa` at the same exact contract total, package pin 158 / 6,245,074 / `b3113c05daaa5d2e870f204083923434456580fa6499190421de062ce9cabbd4`, and final `24/24 -> 18/18 -> 46/46`. It returned R8 NO-GO solely for stale operative Git-lifecycle wording and found no separate runtime, security, database, or package blocker. The corrected lifecycle later culminated in accepted technical Production baseline `fea3b2e11c6331eddc1ee091b165427d8e0218d7`, recorded above. Historical/rejected: the follow-up's first run was `4635/4641`; six static lifecycle/documentation checks failed while runtime, database, BE.6, and embedded `18/18` residue were green. No session or data correction was needed. The earlier timeout, bounded red rerun, and scorer-only exit 97 also remain historical/rejected |
| State-neutral lifecycle correction | **Definitive verification green; no GO claimed.** The first execution is historical/rejected at `4639/4641`: one combined lifecycle/history assertion still detected self-expiring review claims outside the primary current blocks, and one evidence-row classifier required the obsolete word `candidate`. Runtime, database, Guided-VR, BE.6, and embedded `18/18` residue checks were green. Those static contracts were corrected; the definitive rerun passed `4641/4641` with `QUALITY-GATES OK`, the five-stage QA rerun was green at the same total, and final postconditions were `24/24 -> 18/18 -> 46/46`. No session or data correction was required |
| Reusable-prompt lifecycle correction | **First verification historical/rejected at `4640/4641`; no GO claimed.** The new negative-fixture group exposed that the lifecycle analyzer rejected qualified review phrases but not the generic `independent review` equivalent. All application, backend, Guided-VR, BE.6, and final embedded `18/18` residue checks were green. The matcher now rejects qualified and generic forms; no runtime, database, session, or package correction was required |
| Exact stale-phrase coverage | **First verification historical/rejected at `4639/4641`; no GO claimed.** The reverse-order matcher was initially too broad and treated clearly historical `pending`/`required` review prose as operative. Every executed runtime/backend probe and the final embedded `18/18` residue gate were green. The matcher is now confined to the original `open independent ... review` word order plus the already covered forward forms; no runtime, database, session, or package correction was required |
| Final phrase-coverage review and handoff predecessor | **Historical reviewed predecessor.** Exact 11-file manifest SHA-256 `c4a4c2b5bd592c00126f06736e8f8587d0de3dde189b506177bd764fddf3a192` received R8 NO-GO solely because the exact `open independent ... review` word order was not rejected; no other blocker was found. The narrowed correction passed `npm test` exactly `4641/4641` with `QUALITY-GATES OK`, full five-stage QA with all exact markers, and final `24/24 -> 18/18 -> 46/46`. Verified pre-handoff manifest SHA-256 `bd9a68ea8b7d2094d9fad54b561ed773852e30686646fcb446e9a3febfba2499` remains predecessor evidence only; the later lifecycle culminated in accepted technical Production baseline `fea3b2e11c6331eddc1ee091b165427d8e0218d7` |
| Fresh-session handoff verification | **First full run historical/rejected at `4638/4641`; no GO claimed.** Three static documentation checks failed because the new manifest values were not all presented with the analyzer's explicit `SHA-256` label and both reusable prompts omitted the literal deployment-authorization denial required by the current prompt contract. All executed runtime/backend probes and the final embedded `18/18` residue gate were green. The labels and prompt denials are corrected; no runtime, database, session, package, or vendor correction was required |
| Handoff full-suite harness interruption | **Historical/rejected; no completion count and no GO claimed.** A subsequent full-suite attempt reached the final embedded `18/18` residue gate but the temporary server for `publicRoadRouteRendering-probe.js` did not become ready on its dedicated port. No listener or CampuSphere Node process remained. The focused probe then passed immediately in both MySQL and Supabase modes; no repository, database, session, package, or vendor correction was required |
| Fresh-session role boundary | **Synchronized grounding-only authority.** The current Codex and Claude Code prompts inventory and reconcile live truth, perform no review or mutation, and stop for the owner. They do not authorize further offline implementation, another pilot, deployment/promotion, or final Milestone 12 closeout |
| Remaining scope | Technical Production baseline `fea3b2e11c6331eddc1ee091b165427d8e0218d7` remains accepted; staged authority commit `db05b549807535840968bf28cdefac4154a6d59d` is unpromoted; future `main` deployments require manual promotion. Pilot review is complete by owner acceptance. An owner-authorized local OFF.2-OFF.5 implementation candidate exists with focused evidence but no Codex GO; independent review, replacement full verification, D6, OFF.6 browser acceptance, and final Milestone 12 GO remain open. The offline candidate must not be pushed, promoted, or deployed before the presentation and a later explicit owner decision |

The separately authorized backup and additive reconciliation were performed
under one-writer control. The additive phase deleted no building, schedule,
route, scene, hotspot, or user. A later bounded correction removed exactly
three visually confirmed redundant MySQL scene-link hotspots through the
supported administrator interface. No direct SQL, cleanup sync apply,
migration 0020, Git push, or deployment was used. The full-suite and
restoration evidence remains historical and authorizes no further mutation.

## Historical/Superseded — 2026-07-30 Evidence Classification

The material below preserves the earlier evidence chain. Terms such as
"current candidate" inside that historical section describe its July 30 scope
only and do not override the table above.

Historical/superseded: before the `0627bf7` deployment, accepted production
evidence included the read-only SEC-51 smoke at deployed runtime baseline
`d422b54393f659125912ec5c84ae7927c2533288`. Repository HEAD
`db034e5581e6f409083a43dcb80fb82b473e0127` is a later documentation-only
commit whose bytes differ from the production runtime commit. The opening
three-file SEC-51 gate candidate has grown into a local authority/gate
correction plus the bounded schedule-audit allowlist repair; it remains
uncommitted and unaccepted. Its `3752/3752`, `3755/3755`, `3760/3760`, and
`3763/3763` rows below are retained only as historical/superseded or rejected
candidates; none is current R8 acceptance evidence.

The first authority/audit/total-consistency execution is rejected evidence:
3,742 of 3,772 checks passed, 30 static contracts failed, exit code was 1, and
no `QUALITY-GATES OK` marker was produced. The later 3,774/3,777
frozen-candidate execution also remains rejected after exactly three
`docs-current` failures, exit code 1, and no `QUALITY-GATES OK`. An earlier
frozen 12-file matrix was recorded here as green `3777/3777`, and that record is
now superseded and rejected: a fresh execution against those exact frozen bytes
exited 1 at `3776/3777` with one static failure,
`cloudinary-docs :: docs contain no JWT/PEM/AWS/long-hex secret values`, because
the then-current `docs/deployment.md` carried an unlabeled 40-hex Repository
HEAD value that no sanitizer allowance recognized. That red execution is
rejected evidence and is not acceptance evidence.

A bounded documentation-only correction then labelled that value as
`Repository HEAD` and preserved the truthful claim that
`db034e5581e6f409083a43dcb80fb82b473e0127` is a documentation-only commit and
gate-work candidate, not a runtime deployment. `scripts/quality-gates.js` was
not changed by that docs-secret-label correction, and the exact frozen 12-file
manifest is pinned below so the matrix is traceable from this document.

A byte-consistent matrix was then executed once against that corrected manifest
and completed green: preflight and postflight both matched 12/12 hashes with
Git, migration, and process state unchanged; `node --check` exited 0 for both
audited sources and `git diff --check` exited 0 with only LF/CRLF advisories;
the logout probe passed `75/75` at exit 0 with zero FAIL/ERROR/SKIP and zero
escaped or literal logout-error lines; standalone `npm test` exited 0 at
`3777/3777` with `QUALITY-GATES OK` present and `QUALITY-GATES FAILED` absent;
`npm run qa` exited 0 with exactly 3,777 contract PASS lines before
`QUALITY-GATES OK` and all five green markers present exactly once; and the
final ordered postconditions were `24/24 -> 18/18 -> 46/46` at exit 0 each. The
two disclosed wrapper-only overmatches caused no application failure or retry.

This byte-consistent evidence is current candidate evidence only. It remains
unaccepted pending an independent read-only review, and it establishes no R8,
SEC-51, deployment, pilot, or Milestone 12 GO.

### Frozen 12-File Manifest (Documentation-Currency Correction Candidate)

This table pins the 12-file candidate set so the "frozen 12-file matrix" claim
is traceable from this document rather than asserted narratively. It is an
integrity record of local bytes only: not a package inventory, not a deployment
record, and not authorization.

Every row except `docs/new-session-grounding-prompts.md` records the
matrix-time value the byte-consistent matrix was executed against. The
`docs/new-session-grounding-prompts.md` row has been updated to its current
post-correction value; its superseded matrix-time value was
`f272934beb5e8b0ceb4612ac4bad29226484440e0204aadd608ea2f48fcfe7d2`. Later
authorized documentation-currency corrections also changed the six authority
documents, `docs/deployment.md`, `docs/security-checklist.md`, and this file, so
their current bytes now differ from the matrix-time values recorded here.
Because recording any correction changes `docs/test-evidence.md` itself, this
file cannot state its own current hash. Any re-executed matrix must therefore be
run against a freshly recomputed manifest rather than against this table.

| File | SHA-256 |
| --- | --- |
| `AGENTS.md` | `bb7c983ecab396624a2de095db681f49ab23ec5285fa02d2910bcdf9d6748a5e` |
| `CLAUDE.md` | `3d18293aa71d55742c063e5cc6830681b2dafea1d2cdc77b3eebf6c11359ab9a` |
| `CLAUDE_HANDOFF.md` | `42e1d9635d5a6f2a622bf3a8f0327ac0b1dde7024f3a230410c2ac66e5c2b79d` |
| `CODEX_HANDOFF.md` | `3248f809728a30584dd9dac1907cdac8961161b03f81e79a2420f6962d418e40` |
| `ROADMAP.md` | `a8d58f23fa9ce8f0644deaa779369e2d910fb9fc678014cf01cb0d160d04958e` |
| `docs/deployment.md` | `00b081cdd09ce75cfe6807de2849316288f5f544e2ab52bf1196e7cd6b88a079` |
| `docs/new-session-grounding-prompts.md` | `910a96c4886ae41cbbf5f2b4f18f2a2a9f2838cdbc52f2f15414d49639f637a3` |
| `docs/security-checklist.md` | `845256b32fe8012fb650836f597384f2bc2605214a1c2404a102f6470d9d19d8` |
| `docs/test-evidence.md` | `b8bad8b4ad2ced902016e8501aff32ed01a85792d7005dc84dbb41c2f418521f` |
| `plan.md` | `7ec6c605596edf857387c5a49c03ff0ca244eb06ec305069598dfaed0ea66385` |
| `scripts/quality-gates.js` | `21900c8877775b533c82dec18f14ee2ee4ba448ae78037d06675083504d0edd7` |
| `services/auditService.js` | `d63861eab1a52c2da7567db1f103e1df6cc05991926ee715734dd511712c3070` |

The `3777` figure above is a transcript-wide PASS-line reconciliation across the
parent quality-gate output plus inherited spawned-probe stdout. It is not an
in-process `makeRecorder` counter, and no new counter is claimed or introduced.

The latest independently verified postconditions are GREEN:
`24/24 -> 18/18 -> 46/46`. The exact leaked Supabase hotspot and sibling
schedule are absent, all four canonical Supabase identities have zero
unexpired sessions, MySQL is clean, and the selected-VR and aggregate frozen
fingerprints are restored. Before the separately authorized 2026-07-30
restoration, the historical state was `22/24 -> 16/18 -> 41/46`; that incident
is superseded and is not current database truth. The current gate/evidence work
remains an unaccepted candidate requiring independent review. No R8,
deployment, pilot, or Milestone 12 GO is current. The owner-created feedback
form is READY external evidence; its URL remains outside Git.

**Restoration evidence correction.** At execution time,
`admin.schedule.delete` was absent from `auditService`'s allowlist, so the
schedule-deletion audit request was refused; `POST /logout` emits no
audit-service event. The present allowlist repair creates no retroactive row and
adds no logout-audit contract. The executor also ran extra read-only probe
passes and wrote persistent Claude memory outside the repository, exceeding its
stated one-run evidence boundary. Those disclosed deviations did not alter the
verified restored database or repository postconditions and are not erased.

## Evidence Rules

- Do not capture real secrets, `.env` contents, private cookies, session IDs,
  Supabase service-role values, OAuth secrets, database passwords, raw logs, or
  private production data.
- Crop or blur browser devtools, request headers, cookies, query strings, and
  terminal output before using evidence in slides or recordings.
- Prefer seeded demo accounts and demo content. If production data is used for a
  live review, document only pass/fail and sanitized notes.
- Keep command evidence to final status lines such as `QUALITY-GATES OK`,
  `DB-PERF-GATE OK`, `[supabase-smoke] PASS`, `IDENTITY-CONSTRAINTS OK`, and
  `found 0 vulnerabilities`.

## Automated Gate Evidence

**Deployed technical Production baseline.** Production at
`https://campusphere-cspc.vercel.app` serves
`fea3b2e11c6331eddc1ee091b165427d8e0218d7` as the accepted technical
Production baseline; Vercel identity is owner-observed. The authorized push
automatically triggered Production. Historical/superseded: `bbb25d0` was an
earlier evidence synchronization, `43627cf` was the Guided-VR runtime/catalog
remediation. Historical/superseded: previously, before `fea3b2e`, Production served
`0627bf78228148e3f989275810c333c16a1f3356`; `d422b54393f659125912ec5c84ae7927c2533288`
and `78d9053c8ce5c2cc7a9ede80326950cfd29a3a53` were earlier accepted baselines.
`Auto-assign Custom Production Domains` is disabled; future `main` deployments
require explicit manual promotion before they replace the live alias.

| Gate | Command | Expected result | Status | Evidence reference |
| --- | --- | --- | --- | --- |
| Full contract suite (Guided-VR catalog-remediation candidate) | `npm test` | Zero `[FAIL]` lines, `QUALITY-GATES OK`, and zero escaped `Logout error:` lines | **4641/4641 PASS — authority-consistency evidence snapshot; exit 0** | Fresh standalone transcript count: exactly 4,641 `[PASS]` lines, `QUALITY-GATES OK`, and zero failed assertions; live Git and the latest external review report control the later disposition |
| Full contract suite (rejected-run analyzer first execution) — historical/rejected | `npm test` | Preserve the fail-closed analyzer defect without promoting the otherwise green runtime matrix | **Historical/rejected: 4640/4641 checks passed; one `docs-current` failure; exit 1; no `QUALITY-GATES OK`** | Every runtime/database/catalog probe and the embedded canonical residue gate were green, including `18/18`. The new analyzer inspected only the first 4,628 mention in the evidence ledger instead of accepting a later exact evidence scope, so the combined live authority assertion failed closed. The analyzer now evaluates every bounded 4,628 scope and requires at least one transcript-faithful exact account. No session or data correction was required; this red run is not promoted |
| Full contract suite (transcript-fidelity correction candidate) — historical/superseded after independent review | `npm test` | Preserve the exact green result without promoting its contradictory authority text | **Historical/superseded: 4639/4639 PASS; exit 0; `QUALITY-GATES OK`** | Exact 34-file manifest SHA-256 `ebf1142c11e3c027c0b3339a6888bc19196936ae3323644d907c68def224c4b4` received commit-readiness NO-GO because current authority repeated the same rejected 4,628-PASS retry with both the exact nine wording failures plus residue and an incorrect lower failure count. Runtime, Guided-VR, BE.6, package, and QA-marker review found no separate blocker. Superseded by the bounded authority-consistency candidate above |
| Full contract suite (transcript-fidelity first attempt) — historical/rejected | `npm test` | Preserve the bounded timeout without promoting partial output | **Historical/rejected: wrapper timeout at 20 minutes; no completion count and no `QUALITY-GATES OK`** | The process was still inside the catalog-wide Guided-VR probe when the wrapper bound expired. Post-timeout inspection found no CampuSphere Node process or listener. Superseded by the completed current row above |
| Full contract suite (transcript-fidelity bounded retry) — historical/rejected | `npm test` | Preserve the red retry and its exact supported cleanup | **Historical/rejected: 4,628 `[PASS]` lines; exit 1; no `QUALITY-GATES OK`** | Nine failures were current-authority wording mismatches caused by replacing “five stages green” with unsupported wording; the tenth was one orphaned canonical MySQL student session left by the timed-out run. After parser-safe wording correction, a fail-closed preflight proved exactly one intended-role MySQL student session, zero canonical MySQL administrator sessions, and zero sessions for all four canonical Supabase identities; `revokeUserSessions()` was invoked exactly once for that student and restored one to zero. No direct session-row delete or broad cleanup occurred |
| Full contract suite (pre-transcript-fidelity remediation candidate) — historical/superseded | `npm test` | Preserve the earlier exit-0 result without inventing a direct PASS-line measurement | **Historical/superseded: exit 0 with `QUALITY-GATES OK`; exact standalone PASS-line count was not captured** | The later QA contract stage counted 4,637, but that did not directly measure the earlier standalone transcript. Superseded by the freshly counted current row above |
| Full contract suite (pre-independent-review remediation candidate) — historical/superseded | `npm test` | Preserve the prior green candidate without promoting it over the current bytes | **Historical/superseded: 4624/4624 PASS; exit 0** | Validated the prior exact 31-file candidate before the demo-authority, sync-guard, and geometry-probe review findings were corrected; not current commit-readiness evidence |
| Full contract suite (bounded review-finding remediation measurement) — historical/rejected | `npm test` | Measure the synchronized total without promoting a red execution | **Historical/rejected: 4623/4624 checks passed; one `docs-current` failure; exit 1; no `QUALITY-GATES OK`** | Runtime, dual-backend catalog, supported fixture cleanup, and embedded `18/18` residue were green. The sole failure was the reusable Codex prompt lacking an explicit review-open sentence within the fail-closed authority scope; corrected before the exact green row above |
| Full contract suite (Guided-VR catalog-remediation pre-disclosure candidate) — historical/superseded | `npm test` | Zero `[FAIL]` lines, `QUALITY-GATES OK`, and zero escaped `Logout error:` lines | **Historical/pre-disclosure: 4609/4609 PASS; exit 0; does not validate the later failure-disclosure documentation bytes** | One `QUALITY-GATES OK`, zero `[FAIL]` lines, and clean embedded `18/18` residue before QA. Superseded by the current exact row after the bounded session correction and fresh matrix |
| Full contract suite (Guided-VR catalog-remediation measurement) — historical/rejected | `npm test` | Measure the synchronized total without promoting a red execution | **Historical/rejected: 4,591 PASS plus eight documentation-only failures; exit 1; no `QUALITY-GATES OK`** | Every runtime/database/catalog probe and embedded `18/18` residue postcondition passed. Failures were confined to secret-label/current routing wording, reusable-prompt currency, two stale-current safety rows, and three unset suite/QA evidence contracts. This run is retained only as measurement evidence |
| M12.P1-D6 admin dashboard analytics package inventory (candidate) | `node scripts/vercelPackageBoundary-probe.js` | Current repository-byte allowlist inventory matches both independent live pins | **168 files, 7,042,705 bytes, aggregate SHA-256 `fe08232edf026edcbd33371df7d484bfaf39e3de0dafe22f5144e18e08efbf2b`; focused package gate `74/74`; registered in-suite package gate not yet executed under this bounded authorization** | Candidate evidence only; not deployment authorization. The `+3` files versus the offline candidate are `services/adminAnalyticsService.js`, `repositories/analyticsRepository.js`, and `public/js/admin/dashboard-analytics.js`; `scripts/adminDashboardAnalytics-probe.js` is denied by the allowlist and is not packaged. **Historical/rejected after the independent review, never accepted:** the first D6 candidate at 168 files, 7,022,574 bytes, aggregate SHA-256 `779d331824026ce0c1c9510e6393790d0a8da508498a395c1e97d9a04c19e7fd`, 15-file manifest `a6202b0f2106f244d58a41fbc1d646f360356df299790d5f88d44fe2729a2bc2`. **Historical/blocked, never accepted:** the OFF.3-OFF.5 2D offline-navigation candidate at 165 files, 6,971,229 bytes, aggregate SHA-256 `e383f2fe708c5233192ec3602727ed2029dbc906df1ad53a75a70f6fa583334b`; candidate manifest SHA-256 `af7a1a333db0653449727ee5b6b7f223606686a05717ef6f107607bd99f04e9c` with package 165 files, 6,970,280 bytes, aggregate SHA-256 `fc5d8bdcc7a6482bd256d4504224018cfc56ba418f56d81babd6e0ec5a4ff783`, superseded because its service-worker header and API guards were incomplete; candidate manifest SHA-256 `60154d93a3a3109a374a80ffeb4e20f8650aaa131b9b4ff97c16b028cade5f2d` with 165 files, 6,969,343 bytes, aggregate SHA-256 `2dd88fede872db81a771a9d7273c8fd0264e2f6006d5eee09f33a1b930400523`, superseded because automatic API caching contradicted the consent-driven boundary; and 165 files, 6,968,875 bytes, aggregate SHA-256 `115dccba1fc4d9707caa5c43cc8bd7f9340bd7d92286513ad562d60af60b100f`, superseded because its probe required OSM caching and omitted adversarial local-photo/OSM cases. Accepted technical Production predecessor remains 158 files, 6,245,074 bytes, aggregate SHA-256 `b3113c05daaa5d2e870f204083923434456580fa6499190421de062ce9cabbd4` |
| Full contract suite (M12.P1 SEC-51 authority/audit/total-consistency second execution) — historical/rejected | `npm test` | Zero `[FAIL]` lines, `QUALITY-GATES OK`, and zero escaped `Logout error:` lines | **Historical/rejected: 3,774/3,777 checks passed, three `docs-current` failures, exit 1; no `QUALITY-GATES OK`** | The frozen candidate passed its hash/Git preflight, syntax checks, and focused logout probe at `75/75`. `npm test` then completed once and failed only the three recorded documentation contracts: `docs/test-evidence.md` did not distinguish the deployed runtime baseline from the documentation-only commit, and both `docs/test-evidence.md` and `docs/deployment.md` carried a claim-scoped stale deployment conflict. Embedded residue and BE.6 reported `18/18` and `46/46`, but stop-on-red prevented `npm run qa` and the final independent `24/24 -> 18/18 -> 46/46`; no retry was run, and clean embedded checks do not promote the red suite |
| Full contract suite (M12.P1 SEC-51 authority/audit/total-consistency first execution) — historical/rejected | `npm test` | Zero `[FAIL]` lines, `QUALITY-GATES OK`, and zero escaped `Logout error:` lines | **Historical/rejected: 3,742/3,772 checks passed, 30 failed, exit 1; no `QUALITY-GATES OK`** | The single execution exposed static documentation/analyzer contract failures plus the long-hex false positive. It did not establish acceptance evidence and was not retried. The focused logout probe had already passed `75/75`, and the test transcript emitted zero escaped logout-destroy error lines; those facts do not convert this red suite into a pass |
| Full contract suite (M12.P1 SEC-51 gate-hardening correction candidate) — historical/superseded | `npm test` | Zero `[FAIL]` lines, `QUALITY-GATES OK`, and zero escaped `Logout error:` lines | **Historical/superseded: `3760/3760` PASS — later found FAIL-OPEN by independent Codex review on contradictory-scope deployed SHAs and superseded by the correction row above; retained as history, NOT accepted evidence** | `+5` versus the superseded `3755`: five separately named rejecting fixtures for five fail-open cases found by independent Codex adversarial review, which passed only 7 of 12 cases. Two cases escaped topic detection — a stale claim after a semicolon, and a stale claim in the following sentence — because topic matching was per-claim, so a clause that did not repeat the topic was skipped. Three more defeated SHA co-occurrence: a row or prose statement naming a DIFFERENT 40-character SHA as the deployed baseline while mentioning the expected SHA nearby for comparison. Both classes are now closed. Topic is matched on the containing EVIDENCE SCOPE — one table row, or one prose paragraph — so a following claim cannot escape by omitting the topic, while scope boundaries stop context crossing into an unrelated row or paragraph. A new pure `claimBindsShaToDeployedBaseline()` replaces co-occurrence with explicit binding: the claim must be non-historical, contain exactly ONE full 40-character SHA, that SHA must be the expected one, and it must be grammatically attached to the deployed production baseline. Historical and past-bounded exemptions remain per-claim. The pre-existing regression fixture is restored verbatim rather than reworded to suit the detector. Superseded: `3755/3755` was recorded as green but is historical/superseded — independent adversarial review later found it fail-open, so it is retained as history and is NOT accepted evidence |
| Full contract suite (M12.P1 SEC-51 gate-hardening candidate) — historical/superseded | `npm test` | Zero `[FAIL]` lines, `QUALITY-GATES OK`, and zero escaped `Logout error:` lines | **Historical/superseded: `3755/3755` PASS — later found FAIL-OPEN by independent Codex adversarial review (7 of 12 cases passed) and superseded by the correction row above; retained as history, NOT accepted evidence** | `+27` versus the superseded `3728`, reconciled exactly: **14 live** `docs-current` assertions and **13 fixtures**. The live assertions cover the SEC-51 checklist row carrying the host and the exact current baseline; superseded-baseline framing in both evidence documents; both evidence documents separating the deployed runtime baseline from this documentation-only commit; and the stale-claim check across both evidence documents, `docs/deployment.md`, and all six authority documents. **Analysis is CLAIM-SCOPED.** Text is separated into independent claims at cell, sentence, and semicolon boundaries — a soft line wrap is deliberately not a boundary — so a history marker only ever applies to the claim it sits in. Three adversarial fixtures were added for the fail-open this closes, in which a marker elsewhere in the same row or paragraph licensed a false current-deployment statement: a row-level semantic baseline swap, a stale undeployed statement whose marker sat on a neighbouring sentence, and the same swap in combined prose. Each satisfies every substring check — both SHAs and the host present — and each is now rejected. The retained fixtures still accept the current SHA alongside an explicitly historical older SHA, accept past-bounded history wording, accept unrelated archived R5/R6/R7 review prose, and reject rows missing the host or the exact current SHA, old-only rows, deferred rows, duplicates, and absent rows. Disclosed RED attempts: `3751/1` when the deployment-smoke accepting fixture still carried the pre-move baseline, and a hardening pass that legitimately flagged this document's own prose until the wrap-boundary defect was fixed and the fixture description stopped quoting adversarial wording verbatim. No pre-existing gate was weakened. R1-R7 focused probes stay standalone and are not in this total |
| Full contract suite (M12.P1 SEC-51 pilot-surface correction candidate) — historical/superseded | `npm test` | Zero `[FAIL]` lines, `QUALITY-GATES OK`, and zero escaped `Logout error:` lines | **Historical/superseded: `3728/3728` PASS — superseded by the evidence-synchronization row above, which added assertions rather than invalidating this run; retained as history, not as a current disposition** | `+24` versus the superseded `3704`, reconciled exactly: `+11` live `pilot-readiness` assertions (truthful landing mapping; the removed exclusive claim; the unchanged `getRoleFromEmail()` domain comparisons; the navbar ARIA contract; the single deferred shared-client tag; the one-setter public-nav client; no page-local handler on either anonymous view; both views rendering the shared partial; in-card theme placement on both auth surfaces; and the auth-scoped CSS) and `+13` mutated-source rejecting fixtures covering every one of them. The SEC-51 deployment-smoke analyzer was inverted rather than added, so its assertion and fixture counts are unchanged. Disclosed: the first attempt was RED at `3725/3` — the client-behaviour detectors matched the client's own JSDoc header instead of code shapes (fail-open), the landing refusal mutation never matched because the sentence is line-wrapped, and the full 40-char baseline SHA in `docs/deployment.md` tripped the long-hex secret scan. All three were fixed, and a comment-only fixture now pins the fail-open closed. No pre-existing gate was weakened. R1-R7 focused probes stay standalone and are not in this total |
| Full contract suite (M12.P1-R8 evidence-consistency correction candidate) — historical/superseded | `npm test` | Zero `[FAIL]` lines, `QUALITY-GATES OK`, and zero escaped `Logout error:` lines | **Historical/superseded: `3704/3704` PASS — superseded by the SEC-51 pilot-surface correction row above, which added assertions rather than invalidating this run; retained as history, not as a current disposition** | `+19` versus the superseded `3685`, reconciled exactly: `+7` live `docs-current` evidence-consistency assertions (one current non-Pending Full QA aggregate disposition; no Pending or blank disposition anywhere in the Manual Black-Box Checklist; deployment smoke still deferred to SEC-51 and never PASS; no superseded R8 candidate figure in a current status cell, checked in both evidence documents; exactly one current R8 package inventory row carrying the independently pinned figures; and exactly one current full-suite correction-candidate row) and `+12` accepting/rejecting fixtures covering every one of those rules. Each analyzer reads only the markdown text it is handed and targets PARSED CELLS, so a superseded figure quoted as historical context inside an evidence cell stays legitimate while the same figure in a STATUS cell fails. No pre-existing gate changed. R1-R7 focused probes stay standalone and are not in this total |
| Full contract suite (M12.P1-R8 re-review correction candidate) — historical/superseded | `npm test` | Zero `[FAIL]` lines, `QUALITY-GATES OK`, and zero escaped `Logout error:` lines | **Historical/superseded: `3685/3685` PASS — superseded by the evidence-consistency correction row above, which added assertions rather than invalidating this run; retained as history, not as a current disposition** | `+26` versus the previous R8 candidate's `3659`: `+9` in `pilot-readiness` (the scoped anonymous-denial claim, preservation of the separate method/path request-log disclosure, and the ambiguity-reintroduction fixtures) and `+11` in `vercel-package-boundary` (independently pinned stale-worktree-wording detection with positive-neutral and negative fixtures), plus `+6` new runtime `/privacy` assertions across both backends. No pre-existing gate changed. R1-R7 focused probes stay standalone and are not in this total |
| Full QA aggregate (Guided-VR catalog-remediation candidate) | `npm run qa` | Contracts, DB perf, Supabase smoke, identity, and audit all green | **4641/4641 PASS — all five stages green; exit 0** | `QUALITY-GATES OK`, `DB-PERF-GATE OK`, `[supabase-smoke] PASS`, `IDENTITY-CONSTRAINTS OK`, and `found 0 vulnerabilities`; live Git and the latest external review report control the later disposition |
| Full QA aggregate (transcript-fidelity correction candidate) — historical/superseded after independent review | `npm run qa` | Preserve the exact prior five-stage result without promoting the rejected authority bytes | **Historical/superseded: 4639/4639 PASS — all five stages green; exit 0** | The actual markers were `QUALITY-GATES OK`, `DB-PERF-GATE OK`, `[supabase-smoke] PASS`, `IDENTITY-CONSTRAINTS OK`, and `found 0 vulnerabilities`; the exact 34-file candidate nevertheless received commit-readiness NO-GO for its contradictory rejected-run authority wording |
| Full QA aggregate (pre-transcript-fidelity remediation candidate) — historical/superseded | `npm run qa` | Preserve the green inner command while disclosing the evidence-scorer defect | **Historical/superseded: 4637 contract PASS lines; inner `npm run qa` exit 0; enclosing scorer exit 97** | The scorer incorrectly required nonexistent `SUPABASE-SMOKE OK` instead of the actual `[supabase-smoke] PASS` marker. No application stage failed and no retry was caused; the run is superseded by the current transcript-faithful matrix |
| Full QA aggregate (pre-independent-review remediation candidate) — historical/superseded | `npm run qa` | Preserve the prior five-stage result without treating it as current-byte evidence | **Historical/superseded: 4624/4624 PASS — all five stages; exit 0** | Validated the prior exact 31-file candidate; superseded by the current bounded-remediation matrix |
| Full QA aggregate (Guided-VR catalog-remediation first attempt) — historical/rejected | `npm run qa` | Retain a failed chained attempt without promoting partial output | **Historical/rejected: contract stage stopped at 4,594 PASS plus five failed assertions; later QA stages did not run** | Public schedule-display probe exited 1. Its immediate isolated rerun passed MySQL and Supabase, canonical residue remained `18/18`, and the single disclosed full-QA retry passed |
| Full QA aggregate (M12.P1 SEC-51 gate-hardening attempt against the `3755` candidate) — historical/superseded, RED | `npm run qa` | Contracts, DB perf, Supabase smoke, identity, and audit all green | **RED — 17 failures, exit 1; NOT accepted evidence** | Executed once against the prior `3755` candidate bytes and retained here rather than erased. All 17 failures were confined to the Supabase leg of the `adminCampusMapSearchFilter` probe (16 assertions plus its exit-1 line). The same probe exited 0 inside the `npm test` run minutes earlier, and the earlier attempt separately hit `[publicRoadRouteRendering-probe] FATAL: fetch failed` on the Supabase leg, so the condition presented as transient and external. It was NOT reproduced as an application defect, and no application code or probe was changed to address it. The run was not retried. The run's own embedded residue gate still reported `18/18` and the independent ordered postconditions were `24/24 -> 18/18 -> 46/46`; **clean postconditions do not convert a red QA run into passing evidence**. The earlier `3752/3752`, `3728/3728`, `3704/3704`, and `3685/3685` contract totals are historical/superseded and preserved in their own rows above |
| M12.P1 SEC-51 pilot-surface correction (three findings) | `pilot-readiness` gate inside `npm test`, local browser acceptance at 1440x900 and 390x844, and production smoke history | Truthful landing role mapping; a shared, accessible anonymous navbar; an auth-scoped in-card theme control | **Present in deployed technical Production baseline `fea3b2e11c6331eddc1ee091b165427d8e0218d7`; original independent acceptance retained** | The corrections remain present on the current deployment. Historical/superseded: before `fea3b2e`, they were also live on `0627bf78228148e3f989275810c333c16a1f3356`; their detailed independent SEC-51 browser acceptance originally ran against `d422b54393f659125912ec5c84ae7927c2533288`. The current bounded anonymous GET-only verification does not restate authenticated profile-modal or schedule-audit coverage. The implementation and original local verification detail remain retained below |
| Independent `npm test` verification of the recorded R8 evidence commit — historical/superseded | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **RED — seven spawned-gate failures; historical/superseded and explicitly NOT accepted evidence** | One independent verification run encountered transient Supabase read unreadability and ended with seven spawned-gate failures. Its ordered postcondition reading was `23/24 -> 17/18 -> 46/46`: exactly one unexpired persisted Supabase session remained for the canonical regression student, while MySQL and the other three Supabase identities were clean. A separately owner-authorized bounded maintenance restoration then reproduced that exact reading, verified the identity resolved to exactly one row with the intended role, and called `revokeUserSessions()` exactly once for that verified identity through `services/sessionRevocation.js` — no direct SQL, no direct session-row deletion, no dataset or account change. An independent SELECT-only recount returned zero unexpired sessions for all four canonical identities, and the ordered postconditions returned to `24/24 -> 18/18 -> 46/46` with the frozen BE.6 fingerprint unchanged. Neither the red run nor the maintenance restoration is promoted into M12.P1-R8 acceptance evidence |
| M12.P1-R8 local authenticated exposure matrix (clean bounded re-execution) | Browser-driven, both runtime modes | Role denial, registration trust, logout isolation, admin CRUD with supported cleanup, map/routing, full/partial VR coverage, and desktop/mobile overflow/CSP/console checks, with a separate fresh browser context per role | **126/126 PASS, zero failures — MySQL 34/34 plus a 14/14 supplement; Supabase 64/64 plus a 14/14 supplement** | Clean bounded re-execution under a separate owner authorization. It replaces the contaminated first execution recorded in the row below, which is retained as history and is not accepted. A separate fresh `BrowserContext` per role, each proven to carry zero cookies and zero web storage BEFORE authentication. Every authenticated session was registered with `scripts/probeSessionLifecycle.js` immediately after login and terminated exactly once through `terminateAll()` and the real CSRF-protected `POST /logout`, each with a former-cookie replay-denial proof. No `429` occurred; no failed logout was retried; `services/sessionRevocation.js` was never imported or called; no session row was deleted directly and no database cleanup was performed. MySQL exercises administrator and student (the only deterministic local fixtures); instructor and guest are exercised in Supabase, where all four regression identities exist. The supplement covers SEC-08 (the session identifier changes across authentication), SEC-09 (a tampered cookie AND a replayed pre-logout cookie are both rejected), SEC-10 (four SQL-like search payloads leak no SQL/driver/stack text), and SEC-11 (a stored script-and-onerror payload does not execute, is never emitted as a raw script element, and raises zero CSP violations). Every fixture was deleted through the same admin API that created it. Final ordered postconditions were `24/24 -> 18/18 -> 46/46` |
| M12.P1-R8 local authenticated exposure matrix (first execution) — historical/superseded | Browser-driven, both runtime modes | Same scenario set as the clean re-execution above | **Historical/superseded — explicitly NOT accepted evidence, whatever totals it produced** | Recorded so the defect is not silently erased. Repeated matrix runs exhausted the in-memory `preParseAuthLimiter` budget, the run began receiving `429` responses with a `Retry-After`, and one throttled `POST /logout` left an orphaned MySQL administrator session. That session was cleared by calling `revokeUserSessions` DIRECTLY instead of through the supported logout interface. Direct revocation is outside the supported-interface rule this evidence class depends on, so that run cannot be accepted on its numbers. It is superseded by the clean bounded re-execution above, which hit no `429`, retried no logout, never imported or called `services/sessionRevocation.js`, and deleted no session row directly |
| M12.P1-R8 SEC-05 unsupported-domain OAuth flow | Live Google sign-in driven from a fresh anonymous browser context | The domain policy refuses an unsupported domain with a sanitized error and creates nothing | **PASS — externally executed** | The OAuth start reached `accounts.google.com` requesting exactly `openid`, `email` and `profile`. The unsupported-domain Google account completed Google authorization and returned to CampuSphere, which redirected to `/auth?error=unauthorized_domain`. The displayed rejection was sanitized and echoed no email address and no raw error. Supabase `users` held 6 rows before and 6 rows after, with zero rows on unsupported domains; no user row and no role-profile row was created; and no pending OAuth registration persisted. No test email, password, OAuth code, token, cookie, or account identifier is recorded anywhere in this repository |
| M12.P1 SEC-51 schedule-audit correction package inventory — historical/superseded | `node scripts/vercelPackageBoundary-probe.js` | Recomputed after the bounded audit allowlist repair | **Historical/superseded: 158 files, 6,201,747 bytes, aggregate SHA-256 `acfb1696de0c8855e02aa82e243fec959aefec637f29bdf033bc34ffda42e8b1`; focused probe `71/71`** | Superseded by the current Guided-VR catalog-remediation inventory above. Same file count as the preceding pilot-surface candidate; the `+144` bytes were confined to the already-packaged `services/auditService.js`. Before this repair, the historical local candidate inventory was 158 files, 6,201,603 bytes, aggregate `28403afaca31b90849d8cc76c1ec0501f29444d138e865053337617b664d3636`. This inventory described repository bytes only and was never deployment authorization |
| M12.P1-R8 package inventory (re-review correction candidate) — historical/superseded | `node scripts/vercelPackageBoundary-probe.js` | Recomputed after the re-review corrections | **Historical/superseded: 157 files, 6,194,154 bytes, aggregate SHA-256 `77e34105c97bf381cdd207de0b5f4a9abaf7d7d74b68e518c7365cc5e1a8551a`; focused probe `71/71` — superseded by the SEC-51 pilot-surface inventory row above and retained as history** | Same file count as the previous R8 pilot-readiness candidate (157 files, 6,192,992 bytes, aggregate `0ae9f57debf8009235e7bef2160e8320b958e6e873d91d0ffb011a74ab999a1c`); the `+1,162`-byte delta is confined to `views/privacy.ejs`, whose anonymous-denial claim was rescoped. No packaged path was added or removed. The accepted R7 closeout preview (154 files, 6,166,956 bytes, aggregate `c7c16ed73de4b34e1989e6e6842ab897b1164477fb39ddc5862ed1901638b9ec`) remains unchanged historical evidence. The console label is the neutral `CURRENT VERCEL PACKAGE BOUNDARY INVENTORY - NOT DEPLOYMENT AUTHORIZATION`, and the probe now states explicitly that the inventory reflects current repository bytes, does not itself establish Git cleanliness or immutability, and is not deployment authorization. Candidate evidence only — it authorizes no upload, Vercel link, or deployment |
| M12.P1-R8 pilot feedback form readiness | Anonymous read-only fetch of the owner-supplied responder form | Reachable responder page rather than the editor UI, accepting responses, no email collection, and the minimum `docs/usability-survey.md` questionnaire present | **READY — external owner evidence** | Opened anonymously; the page served was the responder page, not the editor UI; it was accepting responses and requested no email address. Matched 10/10 SUS-style statements, 8/8 user-satisfaction questions, and 4/4 open-feedback prompts. Nothing was submitted and no response row was created. The responder URL is deliberately NOT recorded in this repository — only its readiness is |
| Full contract suite (M12.P1-R8 pilot-readiness correction candidate) — historical/superseded | `npm test` | Zero `[FAIL]` lines, `QUALITY-GATES OK`, and zero escaped `Logout error:` lines | **Historical/superseded: `3659/3659` PASS — superseded by the current correction-candidate row above; retained as history, not as a current disposition** | `+82` versus the reviewed R8 clean-snapshot candidate's `3577`, reconciled exactly: `+42` new static `pilot-readiness` gate checks, `+32` new runtime R8 assertions (16 per backend — anonymous `/privacy` availability and content, the four `X-Robots-Tag` surfaces, `/robots.txt`, and authenticated `/dashboard`), and `+8` in the `vercel-package-boundary` gate for the neutral inventory label and its rejecting fixtures (`70 -> 78`). No pre-existing gate changed. R1-R7 focused probes stay standalone and are not in this total |
| M12.P1-R8 pilot-readiness gate (in-suite) | `pilot-readiness` gate inside `npm test` | Anonymous `GET /privacy` is registered without `requireLogin` and its handler touches no session and no database; the notice makes every required disclosure and none of the forbidden claims; the footer and both auth surfaces link `/privacy` and contain zero `href="#"` placeholders; the exact `X-Robots-Tag` value is pinned and `pilotNoIndex` is mounted before the static handler and the routes; `public/robots.txt` disallows every crawler; the indexing control is documented as NOT access control; `docs/deployment.md` records the facilitator-mediated pilot model and the shipped `scope: 'openid email profile'` still matches; and `MANUSCRIPT_TEAMDUTCHESS.pdf` is untracked | **42/42** | Every expectation is pinned in `scripts/quality-gates.js` independently of the audited views/routes/docs. 16 rejecting fixtures mutate the REAL sources: a privacy notice missing the CSPC policy URL, the retention window, or the DPO route each fail; appending an invented legal basis, an automatic-deletion promise, or a "we never share" guarantee each fail; and documentation reintroducing invitation-only framing, an OAuth allowlist, a 100-participant cap, or dropping the basic-identity exception each fail. The placeholder scan covers whole files including comments, so a dead anchor cannot return as commented-out markup |
| M12.P1-R8 pilot-readiness, runtime (in-suite, both backends) | `npm test` live HTTP contract section | Fresh no-cookie `GET /privacy` returns `200` HTML with no session cookie minted and no redirect; the rendered notice carries the operator, capstone nature, every processor, the exact Google scopes, the separate-Google-Form statement, the retention window, RA 10173 rights, the DPO route, and the CSPC policy link, and carries no consent/legal-basis/automatic-deletion claim; `/`, `/auth`, `/privacy`, the authenticated-denial path, and authenticated `/dashboard` `200` HTML each carry exactly `X-Robots-Tag: noindex, nofollow, noarchive`; `/robots.txt` returns exactly `User-agent: *` / `Disallow: /` | **32/32 (16 per backend)** | Exact-value header comparison, so a weakened directive fails. Authenticated coverage uses a real logged-in session rather than a redirect. Indexing control is asserted as a crawler directive only and is never treated as an access-control boundary — `/buildings` still returns `302 /auth` anonymously in the same block |
| M12.P1-R8 package inventory (pilot-readiness correction candidate) — historical/superseded | `node scripts/vercelPackageBoundary-probe.js` | Recomputed after the pilot-readiness correction | **Historical/superseded: 157 files, 6,192,992 bytes, aggregate SHA-256 `0ae9f57debf8009235e7bef2160e8320b958e6e873d91d0ffb011a74ab999a1c`; focused probe `71/71` — superseded by the current inventory row above; retained as history, not as a current disposition** | `+2` files versus the reviewed R8 clean-snapshot candidate (155 files, 6,172,845 bytes, aggregate `d8830164b93819b95b21fca233b8875859b5ce8c1cb1c945162010cdc2fe9e9f`): `views/privacy.ejs` and `public/robots.txt`, both under directories the allowlist already re-includes, so no new packaged path class appears. The accepted R7 closeout preview (154 files, 6,166,956 bytes, aggregate `c7c16ed73de4b34e1989e6e6842ab897b1164477fb39ddc5862ed1901638b9ec`) remains unchanged historical evidence. The console label is now `CURRENT VERCEL PACKAGE BOUNDARY INVENTORY - NOT DEPLOYMENT AUTHORIZATION`, pinned independently in `EXPECTED_PACKAGE_INVENTORY_LABEL` in `scripts/quality-gates.js`; the superseded dirty-worktree label is forbidden outright and its reintroduction fails closed. Candidate evidence only — it authorizes no upload, Vercel link, or deployment |
| Full contract suite (post-D7 logout-probe output-hygiene remediation) | `npm test` | Zero `[FAIL]` lines, `QUALITY-GATES OK`, and zero escaped `Logout error:` lines in the captured transcript | **3529/3529 PASS — independently Codex-accepted additive evidence** | Narrow test/output hygiene only. The mocked failed-destroy unit cases in `scripts/logoutSessionTermination-probe.js` now capture the controller's expected fixed sanitized line rather than printing it, so a green transcript no longer shows what looks like a real logout failure. `controllers/authController.js` is UNCHANGED — a real logout failure is still logged, once, sanitized. `+18` versus the accepted D7 3511: `+7` probe capture assertions (focused `75/75`, baseline `68/68`) and `+11` new `logout-output-hygiene` static gate checks, of which 4 are rejecting fixtures and whose expected line is pinned in `scripts/quality-gates.js` independently of the probe. Independently verified by Codex with 3529 PASS labels, zero `[FAIL]` labels, `QUALITY-GATES OK`, zero escaped logout-error lines, `npm audit --omit=dev` zero vulnerabilities, and postconditions `24/24 -> 18/18 -> 46/46`. Does not supersede the accepted D7 evidence below |
| Full contract suite (accepted M12.P1-D7 closeout) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3511/3511 PASS — accepted M12.P1-D7 Codex GO evidence** | Fresh-context role-isolation rerun accepted: separate Playwright `BrowserContext` objects proved no cookie/localStorage/sessionStorage/IndexedDB/CacheStorage carryover; MySQL and Supabase legs completed through supported application interfaces, cleaned up in reverse dependency order, and restored zero D7 residue. `npm audit --omit=dev` reported zero vulnerabilities; postconditions passed `24/24 -> 18/18 -> 46/46` with frozen fingerprint `a1e11ac03f15f837dade60dead664a88ff30b0bf313a99b760789d079892591d` unchanged. R1-R7 focused probes stay standalone and are not in this total |
| Full contract suite (R3 closeout) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **2921/2921 PASS — accepted Codex GO evidence** | R3 runtime/session-hygiene/ownership/import-detector closeout |
| Full contract suite (superseded R4 candidate) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3021/3021 PASS — superseded by the follow-up below after Codex NO-GO** | +100 vs the accepted 2921: 77 `shared-rate-limit` and 23 `docs-current` checks |
| Full contract suite (R4 follow-up closeout) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3040/3040 PASS — accepted Codex GO evidence** | +19 vs the superseded 3021 (+11 `shared-rate-limit`, +8 `docs-current`); +119 vs the accepted pre-R4 2921 |
| Full contract suite (pre-R5 authority synchronization) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3050/3050 PASS — superseded by the R5 candidate rows below** | +10 vs accepted R4 closeout, entirely from the strengthened `docs-current` authority/status fixtures; R5 was not implemented or run |
| Full contract suite (M12.P1-R5 initial candidate) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3162/3162 PASS — superseded by the R5 follow-up row below** | +112 vs the superseded 3050: +79 new `bounded-anon-denial` checks and +33 retargeted `docs-current` post-R5 predicates/fixtures. R5's own focused probe stays standalone and is not in this total |
| Full contract suite (M12.P1-R5 closeout) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3234/3234 PASS — accepted Codex GO evidence** | +72 vs the superseded 3162: +54 `bounded-anon-denial` checks (global-total contract + database-free helper drives + source-mutation fixtures) and +18 `docs-current` checks (reusable-grounding-prompt extractor/validator + fixtures). R5's own focused probe stays standalone and is not in this total |
| Post-R5-GO / pre-R6 authority synchronization candidate | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **RED — 9 failures; not accepted; no candidate total claimed (superseded)** | The new documentation gate itself was green. Supabase OFF.1 student login and student/admin supported-logout cleanup failed; the embedded residue gate found unexpired canonical administrator/student sessions; the embedded BE.6 gate did not establish Supabase counts/fingerprint or cross-backend equality. No cleanup, revocation, or dataset repair was performed. Closed by a separately owner-authorized restoration, independently reproduced |
| Full contract suite (M12.P1-R6 initial candidate) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **RED — 1 failure; superseded by the row below** | The single failure was in the NEW `self-hosted-vendor` gate, not the application: its `lucide.createIcons` scan matched the `//` comment lines that document the guard — the same class of defect disclosed during R5. No application, session, dataset, or dependency gate failed |
| Full contract suite (M12.P1-R6, pre-documentation candidate) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3369/3369 PASS — superseded by the final row below** | The scan that failed above was rewritten as a precise code shape ignoring lines that BEGIN a comment, with fixtures pinning both directions. The new `self-hosted-vendor` gate contributes `119`; the filesystem-discovering ownership gate adds `5` for the new standalone probe. No pre-change baseline run was taken this session, so the residual difference from the recorded `3234` is not attributed to R6 |
| Full contract suite (M12.P1-R6, documentation-sync candidate) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **RED — 1 failure; superseded by the row below** | The retargeted post-R6 documentation predicate matched a prohibition sentence in `CLAUDE_HANDOFF.md` that named the next section after an imperative verb — a false positive on a ban rather than an authorization. Fixed by rewording the prohibition, not by weakening the gate. No application, session, dataset, or dependency gate failed |
| Full contract suite (M12.P1-R6 pre-remediation candidate) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3375/3375 PASS — superseded by the provenance-remediation row below** | `+6` `docs-current` versus the `3369` row, entirely from the retargeted R6-candidate authority fixtures. `self-hosted-vendor` contributed `119`. R1-R6 focused probes stay standalone and are not in this total |
| Full contract suite (M12.P1-R6 provenance-remediation closeout) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3415/3415 PASS — accepted R6 Codex GO evidence** | `+40` versus `3375`: `self-hosted-vendor` `119 -> 139` (+20 = the exact-inventory assertion replacing the former shape-only one, which contributes no net check, +1 independently pinned inventory-cardinality assertion, +1 independently pinned disk-hash assertion, +17 rejecting inventory mutations, and +1 positive live-manifest anchor) and `docs-current` `+20` (6 evidence-truth assertions plus their fixtures). The remediation adds 17 rejecting mutations plus one positive anchor, not eighteen rejecting entries. R1-R6 focused probes stay standalone and are not in this total |
| R6-GO / pre-R7 authority synchronization (first candidate) | `npm test` | Zero application/data failures; new authority predicates green | **RED — 4 documentation-gate failures; superseded by the closeout row below** | The newly retargeted gate initially failed its blocked-by-R6 negative fixture, the Claude handoff stale scan, and both live/fixture R7 prompt validators. The defects were in the new documentation predicate/prompt-shape assertions, not the application, package manifests, sessions, or dataset. The final embedded residue gate still passed `18/18`. The predicate was strengthened and the R7 validator corrected before rerun |
| R6-GO / pre-R7 authority synchronization closeout | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3415/3415 PASS — current authority synchronization evidence** | Assertion count remains unchanged: the R6-candidate authority and execution-prompt assertions/fixtures were replaced one-for-one by R6-GO/R7-ready and current-R7-prompt assertions/fixtures. This is not R7 implementation evidence. Post-suite safety `24/24`, residue `18/18`, and BE.6 `46/46` with the frozen fingerprint unchanged |
| Self-hosted browser dependencies, static + HTTP (in-suite) | `self-hosted-vendor` gate inside `npm test` | The manifest is verified EXACTLY against `EXPECTED_VENDOR_INVENTORY` — an independently reviewed record that lives in probe code OUTSIDE `public/vendor/manifest.json` and pins, for all five packages and all 18 files, the exact name/version/license/registry-tarball/sha512-integrity/global-interface/source-path/destination/byte-count/final-SHA-256/transformations. Disk AND served (HTTP) bytes are verified against the independently pinned SHA-256, not merely the manifest's own value, so a coordinated bytes-plus-manifest-hash swap fails without an explicit reviewed code change. Also: preserved Leaflet bytes and its single documented transformation; every affected view loads the intended same-origin asset; no remote executable script/stylesheet or `@latest` outside the Google Fonts exception; CSP contraction with nonce/worker/approved origins preserved; stylesheet asset paths resolve locally; every `lucide.createIcons` guarded; truthful map/VR missing-library states; byte-identical package manifests; unchanged service-worker privacy boundary; R1-R6 standalone and residue gate exactly once and last; plus served-asset, 404-fails-closed, and live-CSP HTTP evidence | **139/139** (was `119/119` before the independent-inventory remediation; +20) | Same-analyzer negative fixtures reject a different valid-looking tarball URL, sha512, license, source path, destination, byte count, and SHA-256; missing/extra package and file entries; duplicate package names and destinations; undeclared/altered transformations; a different global interface; and the fail-open closer — a coordinated hash update that still differs from the independently pinned hash. Plus view reference, CSP contraction, standalone-probe registration, and approved-origin allowlist fixtures |
| Self-hosted browser dependencies, focused (standalone) | `node scripts/selfHostedBrowserDependencies-probe.js` | Per backend and per renderer: every affected page references the exact same-origin vendor asset and loads zero external executable scripts; the manifest matches the independently pinned inventory EXACTLY, and every shipped file matches the independently pinned SHA-256 and byte count; every destination served 200 byte-exact; the five Leaflet images resolve; a missing `/vendor` path 404s with no CDN fallback or cross-origin redirect; the live CSP satisfies the full R6 contract; the service worker still approves only tiles + Cloudinary; both canonical sessions terminate through the real logout | **230/230** (was `228/228` before the independent-inventory static checks; +2) | Standalone — never counted in the suite total; MySQL port `3383`, Supabase port `3384`; both renderer modes; run once |
| Full contract suite (accepted M12.P1-R7 Codex GO closeout) | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3495/3495 PASS — accepted M12.P1-R7 Codex GO evidence** | Focused R7 `71/71`; in-suite `vercel-package-boundary` `70/70`; `npm audit --omit=dev` zero vulnerabilities. `+1` versus the historical/superseded `3494`, entirely one new `vercel-package-boundary` fixture assertion proving the audited-source list is pinned by exact ordered equality (`69 -> 70`). No other gate changed. R1-R7 focused probes stay standalone and are not in this total |
| Subsequent 2026-07-26 dependency advisory remediation | `npm ls ejs jake filelist minimatch brace-expansion --omit=dev --all`, `npm audit --omit=dev`, and `npm run qa:audit` | Exact reviewed `ejs@6.0.1`; vulnerable `jake/filelist/minimatch/brace-expansion` production chain absent; zero vulnerabilities | **PASS — current dependency baseline** | Following the accepted 2026-07-22 dependency closeout, a subsequent 2026-07-26 npm advisory drift is remediated: production pins `ejs@6.0.1`, the `jake/filelist/minimatch/brace-expansion` chain is absent, and `npm audit --omit=dev` reports zero vulnerabilities. This later evidence does not rewrite the accepted 2026-07-22 compatible lockfile closeout. No `--force`, override, broad update, application-source change, or migration was used |
| Full contract suite (M12.P1-R7 literal-NUL remediation candidate) — historical/superseded | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3494/3494 PASS — superseded by the audited-source list pinning row above after the independent Codex re-review found the exported-list substitution gap** | `+2` versus the superseded `3492`, entirely the two new `vercel-package-boundary` source-auditability assertions (`67 -> 69`): one live four-file NUL scan and one negative fixture. No other gate changed. R1-R7 focused probes stay standalone and are not in this total |
| Full contract suite (M12.P1-R7 initial candidate) — historical/superseded | `npm test` | Zero `[FAIL]` lines and `QUALITY-GATES OK` | **3492/3492 PASS — superseded by the remediation rows above after the independent Codex R7 review found the literal-NUL auditability blocker** | `+77` versus the accepted R6 `3415`. The new `vercel-package-boundary` gate contributes `67`. The remaining `+10` comes from the retargeted `docs-current` R7-candidate authority/execution-prompt assertions and the one added standalone-accounting assertion in `probe-session-ownership`; the per-gate `docs-current`/`probe-session-ownership` baselines were not re-measured from the R6 run in this session, so that `+10` is reported as a combined figure rather than a split. R1-R7 focused probes stay standalone and are not in this total |
| R7 source auditability (in-suite + focused) | `vercel-package-boundary` gate inside `npm test`, plus the focused probe | Every file in the frozen `R7_AUDITABLE_SOURCE_FILES` set (`.vercelignore`, `vercel.json`, `scripts/vercelPackageBoundary-probe.js`, `scripts/quality-gates.js`) exists and is read as a RAW BUFFER containing zero literal `0x00` bytes, so ordinary `rg`/`grep`/`git diff` return matching source lines instead of "binary file matches". The aggregate separator in `computeAggregateSha256()` is the TEXTUAL `\0` escape, never a literal byte. The independent Codex re-review then found a fail-open substitution gap: the in-suite assertion trusted the probe's exported `R7_AUDITABLE_SOURCE_FILES` wholesale, so swapping `scripts/quality-gates.js` for another existing NUL-free file (e.g. `package.json`) still satisfied a "four existing NUL-free paths" check. The gate now pins the audited-source list INDEPENDENTLY in `EXPECTED_R7_AUDITABLE_SOURCE_FILES` (declared in `scripts/quality-gates.js`, not derived from the probe) and requires EXACT ORDERED EQUALITY with the exported list before accepting the byte scan, which now runs over the independently pinned list | **PASS — 3 in-suite assertions + 1 focused assertion** | Independent Codex review of the initial R7 candidate found one literal `0x00` byte at former line 564 / offset 25235 of the focused probe, which made the whole JavaScript file unsearchable. Remediation replaced that single byte with the two ASCII bytes `0x5c 0x30`; file length grew by exactly one byte, every other byte is identical, and the package preview (154 files, 6,166,956 bytes, aggregate `c7c16ed7…38b9ec`) is unchanged — proving runtime behaviour is identical. `containsLiteralNulByte()` fails closed on any non-Buffer input, including a decoded string that still carries the NUL character; the negative fixture rejects `[0x61,0x00,0x62]`, accepts the textual `[0x61,0x5c,0x30,0x62]`, and rejects `null`/`undefined`/string/array/object-like input. Every NUL-bearing fixture is built at runtime from byte arrays so the gate's own source cannot carry the byte it forbids |
| Vercel package/static-CDN boundary, static (in-suite) | `vercel-package-boundary` gate inside `npm test` | `.vercelignore` is a structurally valid allowlist whose first rule is the root `/*`; every expected root file, runtime directory, public asset class, and vendored runtime file is re-included; `public/img/sample 360` is denied in both the directory and subtree form AFTER the `public` re-inclusion; the enumerated package contains no forbidden path class; `vercel.json` exposes exactly `$schema` and `headers` with the seven reviewed narrowly scoped rules; no build/function/route/rewrite/redirect/framework override; only `/offline.html` declares a static CSP; `middleware/securityHeaders.js` still mints the per-request nonce with `script-src` exactly `'self'` plus that nonce; `server.js` still exports the app and listens only as the main module; no `api/`, `.vercel`, generated manifest, or archive exists; the focused probe stays standalone, database-free, session-free, and bound only to its own dedicated port; and the audited-source list is pinned by exact ordered equality so a probe that swaps a file out of its exported set fails | **70/70** (was 67/67 before the literal-NUL source-auditability remediation, then 69/69; +1 for the audited-source list pinning fixture) | Same-analyzer negative fixtures reject a missing/altered root `/*`; re-inclusion of `.env*`, docs/handoffs, scripts/tests, database/migrations, screenshots, Docker, local-tool, temporary, `node_modules`, and Git paths; re-inclusion or removal of the `sample 360` denial; a missing root file, runtime directory, public asset class, or vendored file; denial reordered before the `public` re-inclusion; duplicate, case-fold-colliding, traversal, absolute, doubled-separator, backslash, percent-encoded, and doubled/leading/trailing-space rules; an extra or missing `vercel.json` top-level key; missing, duplicated, reordered, broadened, or altered header rules; a catch-all or dynamic-route CSP; long-lived immutable caching; and falsified manifest count, byte total, per-file size/hash, aggregate hash, missing label, duplicated path, or backslash path; and a runtime-built fixture proving the audited-source list is rejected on a final-path substitution to `package.json`, a reorder, a same-length duplicate-plus-omission, an extra path, a short list, and null/non-array/non-string input. The expected contract is pinned in probe code OUTSIDE both configuration files, so a coordinated config-plus-preview edit still fails without a reviewed code change |
| Vercel package/static-CDN boundary, focused (standalone) | `node scripts/vercelPackageBoundary-probe.js` | Read-only, database-free, session-free, and external-network-free. Enumerates the package candidate and emits a console-only inventory. **Historical values, retained as R7 history:** at this row's original writing the probe enumerated 154 files, 6,165,772 bytes, aggregate SHA-256 `44172479d5b57f0f7ec9945cc63f9078ad738ba68ad9acf6efc05878dbc5910a` under the then-current label `CURRENT DIRTY-WORKTREE BOUNDARY PREVIEW — NOT AN IMMUTABLE DEPLOYMENT MANIFEST`, and the accepted R7-closeout preview was 154 files, 6,166,956 bytes, aggregate `c7c16ed73de4b34e1989e6e6842ab897b1164477fb39ddc5862ed1901638b9ec`. Those figures and that label are frozen history and are deliberately not overwritten; the CURRENT inventory and the corrected neutral label are recorded in the M12.P1-R8 rows above. Later reviewed remediations change only the current inventory and do not replace accepted R7 evidence. A bounded static listener over a temporary root containing only the allowlisted public files serves representative CSS, client script, PWA icon, web app manifest, offline shell, service worker, and campus image plus all 18 vendored runtime files and the vendor manifest with HTTP 200 and byte-identical bodies; a missing asset, the excluded panorama path in percent-encoded and literal-space forms, every excluded root/scripts/database/docs path, and four traversal forms all fail closed with no redirect and no leaked bytes. Also proves every R7 source file is free of literal NUL bytes and stays text-searchable | **71/71** (was 70/70 before the literal-NUL source-auditability remediation; +1) | Standalone — never counted in the suite total; dedicated port `3385`; run once. Writes no manifest, archive, or `.vercel` metadata into the repository; closes the listener and removes the temporary directory in `finally` |
| M12.P1-R8 clean-snapshot candidate package inventory — historical/superseded | `node scripts/vercelPackageBoundary-probe.js` | Recomputed after every R8 runtime edit and again after the hygiene correction. That candidate was **155 files, 6,172,845 bytes, aggregate SHA-256 `d8830164b93819b95b21fca233b8875859b5ce8c1cb1c945162010cdc2fe9e9f`**. The superseded pre-hygiene candidate was 155 files, 6,172,857 bytes, aggregate `898e1f066d27d6f0619b5253bf61779a858f8e1064bb84756415aab1c444eccc`; the 12-byte delta is the trailing whitespace removed from `views/buildings.ejs`. The accepted R7 closeout preview (154 files, 6,166,956 bytes, aggregate `c7c16ed73de4b34e1989e6e6842ab897b1164477fb39ddc5862ed1901638b9ec`) is unchanged historical evidence and is deliberately not overwritten. Two changes separate them, and neither adds a new packaged path class: (1) `utils/buildingParticipantView.js` (+1 file) — created after the R7 closeout was accepted, so that preview legitimately predates it. It is live runtime source, not verification residue: `controllers/buildingsController.js` requires it for the participant Buildings page's display-only additional-details shape and `scripts/buildingDetailsEditor-probe.js` exercises the same exported helpers; it performs formatting only, with no database access, network call, credential, or stored-contract change. (2) A byte delta inside already-allowlisted files, from the admin-dashboard truthfulness correction to `views/admin/index.ejs` and `controllers/adminController.js` plus the hygiene correction to `views/buildings.ejs` | **Historical/superseded: `71/71` — candidate evidence, not accepted upload evidence; superseded by the current R8 package inventory row above and retained only as history** | Standalone; dedicated port `3385`. This inventory describes the COMPLETE clean-snapshot candidate tree — the committed repository state, not a dirty working directory. Its commit SHA is reported externally in the session report rather than embedded here, because a commit cannot contain its own identifier. It remains candidate evidence until an independent read-only `M12.P1-R8` review decision accepts it; it authorizes no upload, Vercel link, or deployment. The boundary contract is unchanged — same allowlist, same seven header rules, same static-only `/offline.html` CSP, and Express still holds sole authority over the per-response nonce CSP for dynamic responses |
| Admin dashboard analytics truthfulness (in-suite) | `admin-dashboard-analytics` gate inside `npm test` | REPLACES the R8 `admin-dashboard-truthfulness` sample-data disclosure gate, which existed only because the charts were fabricated. The D6 gate drives the REAL service with mocked adapters (no database, no server, no browser) and asserts: exactly 12 strictly ordered Asia/Manila calendar months that tile without gap or overlap, with UTC+08:00 boundaries provably different from UTC ones; exact year rollover and a 29-day leap February; half-open bucketing where a boundary row is counted exactly once; exactly the four role keys `student-cspc`, `instructor`, `admin`, `guest`; a genuine empty backend reporting real zeroes with state `ready`; a failed read reporting `null` everywhere with state `unavailable` and the single fixed sanitized message, leaking no host, key, SQL, or stack. Statically it requires both charts to be `role="img"` with `aria-labelledby` + `aria-describedby` and a semantic table equivalent carrying a caption, column headers, and row headers; the client module to redraw on container resize and on `data-theme` mutation; no `innerHTML`/`eval`/`document.write`/`Function`; no interpolation inside any inline script; no analytics SQL in the dashboard action; `requireRole('admin')` declared before `GET /admin`; the authenticated-HTML no-store policy intact; and no public analytics/metrics/telemetry route | **PASS — focused execution `113/113` after the independent-review correction; not yet executed inside a full `npm test` run under this bounded authorization** | Rejecting fixtures mutate the REAL source: a reintroduced `{ date, users }` or `{ value, color }` array, a chart that loses `aria-describedby`, a table whose element is renamed or that loses its caption or row headers, a client module without a `ResizeObserver` or that stops watching `data-theme`, a client module using `innerHTML`, analytics SQL moved back into the dashboard action, and a declared public analytics endpoint are each rejected, alongside accepting controls over the untouched source. Forbidden tokens `mapViews`, `totalMapViews`, `Sample data`, `admin-chart-sample-note`, and `Live preview` must be absent from both the controller and the view. **Review-correction additions:** the exact count parser must reject every malformed shape and never return 0 for one; the strict role parser must reject a missing key, an extra key, and a negative, fractional, non-numeric, or unsafe value; the four role counts must sum to the total; an invalid total or an out-of-window row must make its side unavailable; `Number(...) \|\| 0` must be absent from the D6 repository and service; the repository must build no backend at import time; Supabase paging must select `id, created_at`, order `created_at` then `id` ascending, page with a ceiling, and never return an id — each with a rejecting mutation; the pinned light/dark palettes must match the client and the view tokens and clear 3:1 against their own surface; the four SVG pattern encodings must be declared, referenced per segment, and mirrored by the legend; and the registered stage must spawn the ORDINARY probe, never `--static-only` |
| M12.P1-D6 real admin dashboard analytics (focused) | `node scripts/adminDashboardAnalytics-probe.js` | Server-free, browser-free, session-free; database access SELECT-only. Covers Asia/Manila month arithmetic, year rollover, leap-year February, start-boundary inclusion, end-boundary exclusion, exactly 12 ordered points, invalid timestamps, genuine zero data, sanitized backend failure, exact four-role normalisation, MySQL/Supabase/mixed composition over mocked adapters, static truthfulness, real EJS rendering in ready/zero/unavailable states, non-admin denial without analytics leakage, mock-DOM resize and theme redraw contracts, and an independent comparison against reachable read-only backends | **PASS in STATIC-ONLY mode — `247/247`, marker `D6-STATIC-ONLY-PROBE OK`. The ordinary database-backed mode was NOT executed under this bounded authorization.** | The probe authenticates nobody: it neither requires `scripts/regressionCredentials` nor issues a `/login` request, so it owns no session. Its audit sink is swapped for an in-memory recorder only inside the denial section and restored afterwards, so requiring the module has no side effect and the wrong-role denials mutate nothing. **Fail-closed comparison contract (review correction):** the ordinary mode REQUIRES both the MySQL and the Supabase comparison legs. An unreachable, unconfigured, or skipped leg records no PASS, fails the run, and suppresses `ADMIN-DASHBOARD-ANALYTICS-PROBE OK`; `PROBE_SKIP_SUPABASE=1` is read as a rejected skip REQUEST, never as permission. `--static-only` runs the pure sections only, initializes no database (the MySQL pool module is replaced in the module cache before anything can load it), and can never print the ordinary marker; the registered suite stage spawns the ordinary mode. **Historical/rejected after the independent review:** the earlier `132/132` execution, whose MySQL leg was reported `NOT EXECUTED` while the run still reported success |
| Supabase exact-session logout remediation (pre-D7) | `node scripts/logoutSessionTermination-probe.js` | Normal delete is one request with no readback. On an ambiguous delete error/rejection, the store checks only the exact SID, accepts confirmed absence, or performs exactly one retry and one final exact-SID readback before a fixed sanitized terminal error. No user-wide revocation fallback, raw error disclosure, timer, or log is added | **PASS — focused MySQL and Supabase runtime contracts plus five database-free retry fixtures** | Implemented after the pre-D7 full-suite candidate exposed another unexpired Supabase administrator session. The later accepted D7 closeout supplied covering evidence: `npm test` `3511/3511`, audit zero, and postconditions `24/24 -> 18/18 -> 46/46` with the frozen fingerprint unchanged |
| R6 independent browser closeout | Desktop `1440x900`, mobile `390x844`, and fresh-context missing-family interception | All eight affected admin pages, `/home`, `/dashboard`, `/about`, `/events`, both `/map` renderers, Free Roam VR, and a valid CAS guided route load from same-origin vendor assets without CSP violations, unexpected errors, or overflow; Lucide, Iconify, Leaflet, Pannellum, and MapLibre failures degrade truthfully without executable CDN fallback, stale routes, or false arrival | **PASS — accepted R6 Codex GO evidence** | Independent Codex run; only expected same-origin interception `404`s occurred; post-browser safety `24/24`, residue `18/18`, and BE.6 `46/46` with the frozen fingerprint unchanged |

Pre-acceptance disclosure for the authority synchronization: two earlier
`npm test` runs were red only in the newly authored `docs-current` R5-ready
assertions (7 failures, then 5). The predicate/prose defects were corrected
before the final `3050/3050` green run; no application, session, dataset, or
dependency gate failed in those two runs.
| Shared rate limiting, static (in-suite) | `shared-rate-limit` gate inside `npm test` | Exact `@upstash/redis@1.38.0` pin, preserved limiter scopes/defaults/429/`Retry-After`, atomic `EVAL`, no Vercel Map fallback, HMAC-only keys, no browser-exposed Upstash variable, plus `retry: { retries: 0 }`, the key-locking shebang/single-key contract, and the no-TTL-clamp guard | **88/88** (was 77/77; +11) | M12.P1-R4 accepted Codex GO evidence |
| Shared rate limiting, focused (standalone) | `node scripts/sharedRateLimit-probe.js` | Database-free and network-free; adapter selection, shared counters across simulated instances, atomic increment + expiry, expiry recovery, count boundary, fixed 429/503, HMAC privacy, leak scans, plus the real-SDK single-attempt retry proof, authoritative-TTL regressions, and key-locking assertions | **180/180** (was 154/154; +26) | Standalone — never counted in the suite total |
| Bounded anonymous access-denial, static (in-suite) | `bounded-anon-denial` gate inside `npm test` | Neither anonymous branch audits; exactly one authenticated-only helper call; the actor guard refuses null/malformed/roleless actors; fixed taxonomy and query-free target; preserved 302/401/403 contracts; no raw IP/header/body/cookie/session-id, Redis denial key, new table, migration, timer, retry, or aggregation path; R1-R5 stay unregistered; residue gate stays exactly once and last. R5 follow-up: the probe's REAL `validateLogsBody`/`toCount`/`readStableGlobalTotal`/`globalTotalStaysAt` are driven database-free with negative fixtures, and source-mutation fixtures prove the authoritative-global-total contract (summary.total read, distinct filtered count, single baseline before both batches, single postcondition after, retained filtered assertion, validator not bypassed) | **133/133** (was 79/79; +54) | M12.P1-R5 accepted Codex GO evidence; negative fixtures mutate the real probe and `middleware/roleAuth.js` sources |
| Bounded anonymous access-denial, focused (standalone) | `node scripts/boundedAnonymousAccessDenial-probe.js` | Per backend: a stable authoritative unfiltered `system_logs` baseline is captured before the anonymous batches; ten anonymous `GET /dashboard` stay `302 -> /auth`, ten anonymous JSON `GET /admin/api/logs` stay the exact `401`, those twenty add zero rows of ANY taxonomy (authoritative `summary.total` unchanged) and zero `authorization/access.denied/denied` rows (filtered, defence in depth), one authenticated student `403` adds exactly one sanitized row, one invalid login adds exactly one `authentication/login.local/failure` row, both sessions terminate through the real logout | **90/90** (was 86/86; +4 = +2 per backend: stable global baseline + zero-of-any-taxonomy) | Standalone — never counted in the suite total; MySQL port `3381`, Supabase port `3382`; run once |
| Canonical session residue (in-suite, final gate) | `node scripts/probeSessionResidue-probe.js` | SELECT-only; zero unexpired canonical sessions in MySQL and Supabase; fails closed when a store is unreadable/unconfigured | **18/18 — current** | Current bounded restoration reverified exactly two sessions for the intended-role canonical MySQL student and zero for the other canonical identities; one supported `revokeUserSessions()` call removed both. Postflight and the ordered matrix are clean; no direct session-row deletion occurred |
| with-server environment resolution (in-suite) | database-free gate inside `npm test` | Child `SESSION_STORE` follows the normalized mode when omitted; ambient value never leaks; blank/invalid fails closed | **14/14** | R3 session-hygiene follow-up |
| Awaited runtime/session bootstrap (standalone) | `node scripts/vercelRuntimeSessionBootstrap-probe.js` | Single-flight readiness; deterministic Express arrival barrier; fixed sanitized 503 | **86/86** | Standalone — never counted in the suite total |
| Vercel production profile (standalone) | `node scripts/vercelProductionProfile-probe.js` | Fail-closed Supabase-only profile, plus the M12.P1-R4 shared rate-limit variables | **119/119** (was **88/88** before the R4 additions; +31) | Standalone — never counted in the suite total |
| Pilot credential/session safety (standalone, R1) | `node scripts/pilotCredentialSafety-probe.js` | Read-only; zero unexpired sessions for all four canonical identities | **24/24 — current** | Standalone — never counted in the suite total. Fresh after the bounded supported Supabase student-session restoration and again in the final ordered matrix |
| Pilot credential/session safety (standalone, R1) — historical/superseded | `node scripts/pilotCredentialSafety-probe.js` | Read-only; zero unexpired sessions for all four canonical identities | **Historical/superseded: 22/24 RED** | The 22/24 reading followed the pre-R6 nine-failure RED synchronization run, whose Supabase logout failures left unexpired administrator/student sessions. That blocker was closed by a separately owner-authorized restoration, independently reproduced; the current result above is 24/24. This row is retained only as history |
| Critical-flow contracts | `npm test` twice consecutively | MySQL and Supabase modes pass with zero `[FAIL]` lines, including FAQ CRUD and all spawned routing/schedule probes | RF.6 PASS (2026-07-13) | Codex closeout run |
| Full QA aggregate (RF.6-era placeholder) — historical/superseded | `npm run qa` | Contracts, DB perf, Supabase smoke, identity, audit all pass | **Historical/superseded — replaced by the current Guided-VR catalog-remediation Full QA row above; retained only so the earlier unexecuted placeholder is not silently deleted** | The current candidate is recorded in the exact 4,641-check row above; this placeholder carries no independent disposition |
| Road-routing focused probes — historical RF.6 closeout | Run the eight route topology, geometry data/API, public rendering, admin editor, map/VR, Free Roam, and VR schedule probes | At RF.6 closeout, both backends verified 20 nodes, 48 directed edges, 24 reverse pairs, 48 valid geometries, and 13 routable destinations | **Historical accepted RF.6 evidence (2026-07-13); superseded for current-byte counts by the catalog-remediation candidate above** | Codex closeout run |
| DB performance/indexing | `npm run qa:db` | MySQL hot paths indexed; Supabase migrations `0001` through `0019` declared/owner-applied with live 48/48 route-geometry coverage | RF.6 PASS (2026-07-13); BE.5 parity PASS (2026-07-17) | Codex closeout run |
| Supabase smoke | `npm run qa:smoke` | PASS when configured, SKIP only on dev machines without Supabase env | RF.6 PASS (2026-07-13) | Codex closeout run |
| Identity constraints | `npm run qa:identity` | MySQL and Supabase uniqueness checks pass | RF.6 PASS (2026-07-13) | Codex closeout run |
| Dependency-security remediation | `npm audit --omit=dev` and `npm run qa:audit` | Zero vulnerabilities; `body-parser@2.3.0`; `brace-expansion@2.1.2`; no `package.json` change | **PASS (2026-07-22) — accepted Codex GO evidence** | Compatible transitive lockfile-only remediation; full suite remained 3040/3040 |
| Docker package smoke | `docker build -t campusphere:m9 .` then `docker run --rm campusphere:m9 node --check server.js` | Build succeeds and syntax check passes | Milestone 13 Docker finalization (run on a Docker-enabled machine) | Docker unavailable in Codex environment |

## Manual Black-Box Checklist

| Area | Scenario | Steps | Expected result | Status | Evidence reference |
| --- | --- | --- | --- | --- | --- |
| Local login | Student login | Sign in through the real `/auth` form as the canonical regression student identity (resolved from the test-only loader; values are never recorded here) | Redirects to `/dashboard`; student dashboard renders | **PASS (clean bounded matrix)** | M12.P1-R8 clean bounded exposure matrix, both runtime modes, `126/126` with zero failures. Sign-in used the real form from a fresh browser context carrying zero cookies and zero web storage; the session was registered with `scripts/probeSessionLifecycle.js` immediately after login and terminated exactly once through the real CSRF-protected `POST /logout` |
| Local login | Admin login | Sign in through the real `/auth` form as the canonical regression administrator identity (resolved from the test-only loader; values are never recorded here) | Redirects to `/admin`; admin dashboard renders | **PASS (clean bounded matrix)** | M12.P1-R8 clean bounded exposure matrix, both runtime modes, `126/126` with zero failures. The administrator context reached `/admin` and `/admin/api/*` with `200`, in a separate fresh context with no carryover from any participant role |
| Logout | POST logout flow | Use the real CSRF-protected logout form | Session destroyed; redirects to `/auth?logged_out=1`; `/admin` denied afterward | **PASS (clean bounded matrix)** | M12.P1-R8 clean bounded exposure matrix, both runtime modes. Every authenticated session was terminated exactly once through the real `POST /logout`, each with a former-cookie replay-denial proof, and the post-logout request was denied. No `429` occurred, no failed logout was retried, `services/sessionRevocation.js` was never imported or called, and no session row was deleted directly. Final ordered postconditions were `24/24 -> 18/18 -> 46/46` |
| Google OAuth fallback | Missing OAuth config | Leave OAuth client id/secret unset and open `/auth/google` | Redirects to `/auth?error=oauth_failed`; local login still works | **NOT EXECUTED — documented behavior only; NOT part of the configured pilot evidence and not counted as passing** | The pilot environment is deliberately OAuth-configured, so the unconfigured-fallback branch was never exercised as live evidence. The behavior is documented in `controllers/authController.js`, but no live run exists, so no PASS is claimed for this row |
| Google OAuth role mapping | Domain rules | Drive the live Google flow and observe the domain decision | `@my.cspc.edu.ph` -> student, `@cspc.edu.ph` -> instructor, `@gmail.com` -> guest; other domains rejected | **PARTIAL — unsupported-domain refusal externally executed (SEC-05); approved-domain sign-ins NOT executed** | Only the unsupported-domain branch has live evidence, recorded as SEC-05 in `docs/security-checklist.md`: the flow reached `accounts.google.com` requesting exactly `openid`, `email` and `profile`, returned to CampuSphere, and was refused at `/auth?error=unauthorized_domain` with a sanitized message, creating no user row, no role-profile row, and no persisted pending OAuth registration. No approved-domain sign-in was executed, so the three approved-domain mappings are explicitly NOT claimed as executed; `getRoleFromEmail()` remains covered only by source review |
| Role access | Guest/student/admin boundaries | Try admin URLs as student/guest/instructor/anonymous | Anonymous gets 401/redirect; non-admin gets 403; admin succeeds | **PASS (clean bounded matrix)** | M12.P1-R8 clean bounded exposure matrix, both runtime modes: `/admin` returned `403` HTML and `/admin/api/*` returned `403` JSON for every non-admin role, `200` for the administrator, and `302`/`401` for anonymous. Reinforced in-suite by the `bounded-anon-denial` gate `133/133` and the standalone R5 probe `90/90` |
| Registration trust | Public local registration | Try local registration with `student-cspc`, `instructor`, or `admin` role | Public local sign-up creates guest only; institutional/admin self-registration rejected | **PASS (clean bounded matrix)** | M12.P1-R8 clean bounded exposure matrix, both runtime modes: escalation to `admin`, `instructor`, and `student-cspc` was refused and public local sign-up produced a `guest` account only. Enforced twice — at the controller and again in `0009_public_registration_trust_policy.sql` |
| Admin CRUD | News/events/FAQs | Create, edit, list, delete a demo item | JSON contracts return `{ success: true }`; UI reflects changes | **PASS (clean bounded matrix)** | M12.P1-R8 clean bounded exposure matrix, both runtime modes: admin CRUD completed create -> read-back -> edit -> delete with a real list read proving zero residue. Every fixture was deleted through the same admin API that created it |
| Admin CRUD | Invalid payload | Submit missing/invalid required fields | Sanitized 400 JSON/HTML response; no raw stack or SQL | **PASS (automated)** | Named live contract assertions inside `npm test`, in both runtime modes: malformed JSON returns `400 {success:false}`, an invalid FAQ payload returns `400 {success:false}`, and invalid building/VR-scene `image_url` and `cloudinary_public_id` values each return `400`. The same section scans every captured error body for raw stack, SQL, and driver text |
| Map search | Search known building/route | Search a seeded building and route term | Results appear, capped, no private data | **PARTIAL — cap enforcement and injection safety proven; a positive "results appear" browser search was not separately recorded** | `npm test` asserts the over-cap contract (`/api/search` with an over-length query returns `400 {success:false}`) in both runtime modes, and the matrix SEC-10 supplement drove four SQL-like search payloads that leaked no SQL, driver, or stack text. Neither source records a positive seeded-term result set as a distinct measured check, so the "results appear" half of this row is not claimed as executed |
| Route/pathfinding | Road-following destination route | Select destinations from the map and compare the drawn line against the API response | The line starts at the Guard House, matches API `route.geometry`, follows campus roads, and steps match `route.segments` | **PASS (clean bounded matrix + named automated probes; current catalog-remediation candidate refreshed)** | M12.P1-R8 clean bounded exposure matrix exercised destination routing from the map in both runtime modes and found it truthful. The in-suite road-routing probes (topology, stored geometry, API assembly, public Leaflet/MapLibre rendering, admin geometry editing, and map-to-guided-VR flow) assert the Guard-House origin, geometry/segment correspondence, and cross-backend parity; the standalone SELECT-only `routeGeometryData-probe.js` independently verifies the backend-specific complete graph, geometry, reverse-pair, and routability counts. The refreshed expanded BE.6 candidate remains `46/46`: MySQL has 34 buildings, 44 route nodes, 100 directed edges, 50 exact reverse pairs, 100 valid geometries, and 33 routable destinations; Supabase has 25 buildings, 26 route nodes, 50 directed edges, 25 exact reverse pairs, 50 valid geometries, and 25 routable destinations; the shared Guided-VR catalog has 25 active destinations, 472 configured steps, and 99 unique scene keys |
| VR route | Guided scene coverage and hotspots | Open a fully mapped route and a partially mapped destination | Hotspots work; arrival requires the configured natural destination, stored endpoint mappings, approved delivery metadata, and exact bidirectional adjacent-scene links; any incomplete contract ends with an explicit notice | **PASS (clean bounded matrix + named automated probes)** | M12.P1-R8 clean bounded exposure matrix confirmed truthful Free Roam responses in both runtime modes. The catalog-wide Guided-VR probes establish the configured natural destination node, stored start/arrival mappings, approved Cloudinary URL/public-ID metadata, exact forward/reverse adjacent links, and fail-closed incomplete-route behavior; `vrScheduleHotspot` separately covers schedule hotspot behavior, empty states, leak boundaries, and cleanup |
| PWA/offline | OFF.2-OFF.5 local 2D candidate | Verify install/update lifecycle, explicit integrity-checked download, normal local campus map, Main Gate routes, node/list details, privacy exclusions, logout removal, and fallback behavior | Authenticated HTML remains network-only; no schedule/VR/media/private data enters offline storage; candidate remains local pending independent/final gates | **Candidate focused evidence after the header/guard-exactness correction: OFF.2 `145/145`; 2D offline navigation `35/35`; package boundary `74/74`; no GO claimed.** The `public/sw.js` header is now truthful (only shell + static caches exist; only exact reviewed shell assets are cache eligible; all cross-origin and all same-origin `/api` requests are network-only; `API_CACHE`/`EXTERNAL_CACHE`/approved-host caching/synthesized API fallbacks do not exist; successful-response and redirect rules apply only to reviewed shell/static caching) and that edit is documentation-only — the comment-stripped `public/sw.js` SHA-256 is unchanged at `fc043eae18d05710f94539a1434131a5a8132401c3dfdd0a1f92cf1b306feb6d`. The OFF.2 analyzer, quality gate and self-hosted probe each now require exactly one `/api` prefix, the full classifier truth table evaluated behaviourally in an isolated `node:vm`, `CURRENT_CACHES` tokenizing to exactly `[SHELL_CACHE, STATIC_CACHE]`, absent API machinery, and guard-before-strategy ordering, failing closed on extraction/evaluation errors; the compound rejecting fixture also rejects an always-false classifier and a third cache. **Historical/blocked:** manifest `af7a1a333db0653449727ee5b6b7f223606686a05717ef6f107607bd99f04e9c` is superseded and was never accepted, because its header and API guards were incomplete. Prior correction detail retained below. Automatic API caching is removed entirely: `API_CACHE`, `API_MAX`, `isApprovedApi()`, `apiStrategy()`, the synthesized offline JSON response and the approved-API fetch branch are gone, `CURRENT_CACHES` holds only shell + static, and a single `/api` network-only prefix covers `/api/buildings`, `/api/routes`, `/api/routes/*`, `/api/vr/routes/*`, `/api/search`, `/api/pathfind` and every query-string variant. Ten adversarial API requests against a SUCCESSFUL online emulated worker — whose `/api/buildings` body carried both a Cloudinary image URL and a local building-photo reference — produced zero `respondWith()` calls, zero cache mutations and byte-identical cache contents, with rejecting fixtures for restored constants, classifier, strategy, interception branch, a narrowed `/api` prefix, `API_CACHE` back in `CURRENT_CACHES`, and a stale `v14`. `CACHE_VERSION` advanced once `v14`->`v15`, evicting the v14 shell/static/API caches. **Historical/blocked:** the `145/145`/`35/35`/`74/74` evidence at manifest `60154d93a3a3109a374a80ffeb4e20f8650aaa131b9b4ff97c16b028cade5f2d` is superseded and was never accepted, because it still permitted automatic API caching. | Database-free/static and bounded anonymous harness evidence. Only the exact reviewed shell assets (matched on pathname + query, derived from `PRECACHE_URLS`) are same-origin cache eligible; every other same-origin static/media request and EVERY cross-origin request — including `tile.openstreetmap.org`, its a/b/c subdomains, and `res.cloudinary.com` — is network-only, with the external cache constant, size cap, host classifier, strategy and mode helper removed and dropped from `CURRENT_CACHES`. Online OSM stays CSP-permitted; offline map rendering uses the bundled content-addressed PMTiles archive. That correction advanced `CACHE_VERSION` `v13`->`v14`; the current version is `v15` per the cell above. Runtime proof of zero interception and byte-identical cache contents covers `/img/campus-hero.jpg`, a DB-selected building photo, a percent-encoded `/img/vr/` panorama, a non-shell script, a non-shell stylesheet, a query-modified shell path, OSM apex + subdomains, Cloudinary, and `/api/vr/routes/*`; a positive fixture still accepts all 16 exact shell assets, with rejecting mutations for extension-wide matching, an arbitrary allowlist path, OSM/Cloudinary re-admission, a restored cross-origin strategy, and a stale v13 contract. **Historical/blocked:** the earlier `145/145`/`35/35`/`74/74` evidence at manifest `8c6afdcf817142bb4e57b287f43e836cdd3b089ea2899f65ca88d783c335dd93` is superseded and was never accepted, because that probe required OSM caching and never exercised same-origin photos, non-shell static files, or OSM requests. The prior correction's durable logout deletion and single route-snapshot read path (eight reads to five) are retained. OFF.6 browser acceptance and full regression remain open |
| Mobile layout | Phone viewport | Check auth, dashboard, map, admin key screens at mobile width | No broken overflow or blocked primary actions | **PARTIAL — PASS for `/dashboard`, `/map`, and `/admin`; the anonymous `/auth` surface was not separately measured for mobile overflow** | M12.P1-R8 clean bounded exposure matrix, both runtime modes: `/dashboard`, `/map`, and `/admin` showed no horizontal overflow, zero CSP violations, and zero console errors at 1440x900 and 390x844. The matrix did not record `/auth` as a distinct mobile-overflow measurement, so that screen is not claimed |
| Deployment smoke | Production hostname | Exercise the deployed app read-only on the production hostname | Production serves the synchronized baseline; public pages/assets retain expected contracts; protected surfaces deny anonymous access | **PASS (bounded anonymous read-only GET-only post-deployment verification; owner-observed deployment identity)** | SEC-51 continuity: the bounded checks ran against `https://campusphere-cspc.vercel.app` on deployed technical Production baseline `fea3b2e11c6331eddc1ee091b165427d8e0218d7`. Public pages/assets returned expected responses, sampled bytes matched pushed source, protected HTML redirected to `/auth`, protected JSON returned `401`, and checked responses set no cookie. `/auth` was deliberately avoided; no authenticated login or schedule auditing was exercised. Historical/superseded: before this deployment, anonymous smoke `31/31` ran on `0627bf78228148e3f989275810c333c16a1f3356`; the earlier detailed browser smoke ran on `d422b54393f659125912ec5c84ae7927c2533288` |

**Manual checklist status labelling.** Every row above states HOW it was proven,
and no row is left unrecorded.
`PASS (clean bounded matrix)` means the exact scenario was executed in the
M12.P1-R8 clean bounded exposure matrix (`126/126`, zero failures) in both
runtime modes, entirely through supported application interfaces.
`PASS (automated)` means the exact row is proven by named automated assertions
inside `npm test`, and is **not** a manual execution claim.
`PASS (clean bounded matrix + named automated probes)` means both sources
together prove the row.
`PARTIAL` marks a row where part of the expected result has evidence and the
remainder is explicitly NOT claimed; the unproven half is named in the row.
`NOT EXECUTED — documented behavior only` marks a row with no live evidence at
all; it is not counted as passing.
`PASS (externally executed, independently accepted)` marks the single row the
local harness cannot perform at all — executed by the owner against production
and independently accepted, with the exact host and deployed baseline named in
the row so the claim is checkable. No manual row is marked `Pending`, and no row
remains `DEFERRED`.

## Screenshot And Recording Checklist

Capture only sanitized evidence for:

- Auth page, successful student login, successful admin login, and logout result.
- Student/instructor/guest/admin role boundary examples.
- Admin CRUD create/list/delete flow using demo content only.
- Map search and route/pathfinding result.
- VR route scene and hotspot navigation.
- Offline fallback page and PWA install/manifest indicator if available.
- Mobile auth/dashboard/map/admin layout.
- Terminal final lines for `npm run qa`, `npm run qa:db`, `npm run qa:identity`,
  `npm run qa:smoke`, and `npm run qa:audit`.

Before sharing any image or recording, verify:

- No browser cookies, request headers, session IDs, or OAuth codes are visible.
- No `.env`, Supabase service-role key, DB password, or OAuth secret is visible.
- No private production records or real student data appear.
- No raw stack traces, SQL fragments, or driver errors are displayed.
